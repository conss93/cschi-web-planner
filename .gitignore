node_modules/

# 브라우저 로그인 세션 — 쿠키와 토큰이 들어있으므로 절대 커밋 금지
.auth/

# 수집 결과. 용량이 크고 계정 정보가 섞일 수 있어 커밋하지 않는다.
out/

.env
.env.*
# 값이 비어 있는 견본은 저장소에 둔다. 위 규칙에 걸리므로 예외로 뺀다.
!.env.example
.DS_Store

# Next.js
.next/
.vercel/
